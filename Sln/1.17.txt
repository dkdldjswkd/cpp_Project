// 오전 3:46 2023-01-25

PacketBuffer::Get_PacketSize()
PacketBuffer::Get_Packet()
	-> Lan, Net 패킷마다 다름, 수정해야함 (패이로드 위치 - 헤더 사이즈 리턴함 (Net, Lan 헤더 크기 다름))

릴리즈 세션 시 멤버변수 셋팅?

세션 수, 플레이어 수 제한

*** Packet TLS Pool 대체
 
타임아웃 구현 // 내부 외부?

릴리즈 부 패킷 free 로직 기억하기. (왜 SendQ, SendQ arr 둘다 Free 해야 하는지)

Lan, Net 편하게 사용할 수 있게 하기 (PACKET::alloc_lan X)

고정 Key, 타임아웃 시간 등 파서 로 읽어와서 서버 셋팅하게하기

IOCP 제한 러닝스레드 개수, 워커 스레드 개수 옵션 지정

할당 주소를 재할당하는 문제
	1. 스레드마다 갖고있는 청크가 같았다.
		한 스레드에서 Free 후에도 들고있었다?
	2. 한 스레드에서

JLIB 공통 헤더 만들기?
	옵션 헤더

시스템 로그 남기기

자료구조 컨테이너, LOCK 은 정확히 표시하자.
플레이어 락?

*** {release_flag, io_count} 64 경계 

//

Update 스레드로 JOB을 던지면, io count를 물고있지 않기때문에
SESSION ID로 
but Onrecv로 처리하게 된다면, OnLeave에서 ID를 key로 갖는 Value는 삭제, 재사용될 수 없음
SESSION ID로 접근하는 



1. Player Sector가 언제 수정되는가?
	1. Sector Move 패킷 수신 시
	2. OnClientJoin 호출 시


1 순위


















